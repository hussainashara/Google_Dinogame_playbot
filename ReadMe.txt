Its a google Dino game bot which works by image sensing.

Before running the code you need to install following 3 depedencies(packages) : Pillow, pyautoGUI, numpy

Don't forget to change the cordinates according to your screen.

Open both the chrome and Your python IDE on same screen, side by side.
